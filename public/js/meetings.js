$(document).ready(function($)
{
	/*$('#user_left_menu_cont').on('click', '.next_minute', function(event) {
		event.preventDefault();
		$('#content_right').html('<div class="loading"><span class="glyphicon glyphicon-refresh glyphicon-refresh-animate"></span> Loading...</div>');
		$.ajax({
			url: 'nextminute/'+$(this).attr('mid'),
			type: 'GET',
			dataType: 'html',
			//data: {param1: 'value1'},
		})
		.done(function(output) {
			$('#content_right').html(output);
		})
		.fail(function() {
			
		})
		.always(function() {
			
		});
		
	});*/
});